<Window x:Class="PoputkeeLite.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="200" Width="300"
        WindowStartupLocation="CenterScreen">
        <Grid Margin="20">
            <StackPanel>
                <TextBlock Text="Введите ваш логин:" Margin="0,0,0,5"/>
                <TextBox x:Name="LoginBox" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,15"/>
                <Button Content="Войти" Command="{Binding LoginCommand}" 
                    Padding="10,3" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>
    </Window>
